version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"
    nodejs: "18"
  apt_packages:
    - graphviz
    - npm

  jobs:
    create_environment:
      - asdf plugin add uv
      - asdf install uv latest
      - asdf global uv latest
      - uv venv
      - uv tree
    # install:
    #   - uv pip install -r docs/requirements.txt
    build:
       html:
         - uv run sphinx-build -T -b html -c docs/source/ docs/source $READTHEDOCS_OUTPUT/html

sphinx:
  # Path to your Sphinx configuration file.
  configuration: docs/source/conf.py
