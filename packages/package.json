{
	"name": "buckaroo-packages",
	"private": true,
	"packageManager": "pnpm@9.10.0",
	"scripts": {
		"build:core": "pnpm --filter buckaroo-js-core run build",
		"build:widget": "pnpm --filter buckaroo-widget run build",
		"build": "pnpm build:core && pnpm build:widget"
	},
	"devDependencies": {
		"@biomejs/biome": "1.9.4"
	}
}
